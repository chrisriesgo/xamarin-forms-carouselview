<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
	xmlns="http://xamarin.com/schemas/2014/forms" 
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:cv="clr-namespace:Xamarin.Forms;assembly=Xamarin.Forms.CarouselView"
	x:Class="CarouselLayout.HomePage" Title="No Page Indicator">
	<ContentPage.Content>
			
		<cv:CarouselView ItemsSource="{Binding Items}" x:Name="CarouselItems" ItemSelected="Handle_ItemSelected">
			<cv:CarouselView.ItemTemplate>
				<DataTemplate>
					<Grid BackgroundColor="{Binding Background}">
						<Grid.RowDefinitions>
							<RowDefinition Height="*" />
						</Grid.RowDefinitions>
						
						<Label Grid.Row="0" Text="{Binding Title}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" />
					</Grid>
				</DataTemplate>
			</cv:CarouselView.ItemTemplate>
		</cv:CarouselView>

	</ContentPage.Content>
</ContentPage>
